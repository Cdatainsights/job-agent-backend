{
  "name": "surf-new",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "fastapi-dev": "uv run python -m uvicorn api.index:app --reload --host 0.0.0.0 --port 8000\n",
    "dev":"npm run fastapi-dev",
    "start": "nohup uv run python -m uvicorn api.index:app --host 0.0.0.0 --port 8000 & disown\n"
  }
}
